services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: inshard_example
      POSTGRES_USER: inshard
      POSTGRES_PASSWORD: inshard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
